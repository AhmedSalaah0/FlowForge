@model GroupResponse
@{
    ViewBag.Title = "Editing group";
}

<form asp-controller="Groups" asp-action="EditGroup" asp-route-groupId = "@Model.GroupId" class="add-group-form">
    <div class="form-group">
        <label asp-for="groupTitle">Group Title</label>
        <input asp-for="groupTitle" class="input-field" />
    </div>

    <div class="form-group">
        <label>Choose a color</label>
        <div class="color-picker">
            @for (int i = 0; i < ViewBag.ColorOptions.Count; i++)
            {
                var color = ViewBag.ColorOptions[i] as string;
                <label class="color-option">
                    @if (color == Model.selectedColor)
                    {
                        <input asp-for="selectedColor" type="radio" value="@color" checked />
                    }
                    else
                    {
                        <input asp-for="selectedColor" type="radio" value="@color" />
                    }
                    <span class="checkmark">✔</span>
                    <div class="circle" style="background-color: @color;"></div>
                </label>
            }
        </div>
    </div>

    <button type="submit" class="submit-button">Save</button>
</form>
